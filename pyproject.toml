[project]
name = "site-pulse-audit"
version = "0.1.0"
description = "Strands agent that probes LLM-style answers and drafts citation pages so solopreneur products get named."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = [
  "strands-agents==1.54.0",
]

[project.scripts]
audit-site-pulse = "agent:main"
